<% if policy(Kondo).create? %>
  <%= link_to "New Kondo", new_kondo_path %>
<% end %>

<div class="container">
  <div class="row justify-content-center">
      <div class="col-6">
        <h3>Scroll through your Hood</h3>
        <%= render "form", kondo: @kondo %>
      </div>
  </div>
</div>
